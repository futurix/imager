STRINGTABLE
BEGIN
    // required value for themes (it will change if new incompatible theme system will be introduced in the future)
    1		"FxImgThemeR2"
    
    // name of the theme (in English)
    2		"Example"
    
    // theme description (leave empty if not needed)
    3		"Example theme"

    // author of the theme (you can include your e-mail or url)
    4		"Alexander Tereshchenko"
    
    // last version of FuturixImager this theme was tested with (future versions may add more buttons)
    5		"5.9"
END
